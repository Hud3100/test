<h1>投稿一覧</h1>
<% @posts.each do |post| %>
  <hr>
  <p><span>タイトル:</span><%= post.title %>
  <p><span>内容:</span><%= post.content %>
<% end %>

<% if current_user %>
  <%= form_for @post do |f| %>
    <%= f.label :title, 'タイトル' %>
    <%= f.text_area :title %>
    <%= f.label :content, '内容' %>
    <%= f.text_field :content %>
    <%= f.submit %>
  <% end %>
<% else %>
  <p>投稿の前にログインをお願いします。</p>
<% end %>